#include "terrain.h"

// Collision Terrain::Collision(const Player& player) const {
//   Float2 playerMin = Float2(player.GetLeft(), player.GetBottom());
//   Float2 playerMax = Float2(player.GetRight(), player.GetTop());
//   return Collides(playerMin, playerMax, mMin, mMax);
// }